<ivysettings>
    <settings defaultResolver="cudf"/>
    <!--<caches defaultCacheDir="${basedir}/${dl}/cache">
      <cache name="local" basedir="${basedir}/${dl}/cache" />
    </caches>-->
    <resolvers>
        <cudf name="cudf" pattern="[module]-[revision].[ext]" url="file:test/test-cudf/repositories/cudf-without-url" searchUrl="/[organisation]/[module]/[revision]/[artifact]-[revision].[ext]" m2compatible="true"/>
    </resolvers>
</ivysettings>