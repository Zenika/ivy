<ivysettings>
  <settings defaultResolver="cudf"/>
  <!--<caches defaultCacheDir="${basedir}/${dl}/cache">
    <cache name="local" basedir="${basedir}/${dl}/cache" />
  </caches>-->
  <resolvers>
    <cudf name="cudf" pattern="[module]-[revision].[ext]" url="file:test/repositories/cudf" searchUrl="/[groupId]/[artifactId]/[version]/[artifactId]-[version].cudf" m2compatible="true"/>
  </resolvers>
</ivysettings>
